import { UserConfig } from '../models/user-config.model';

export const defaultUserConfig: Required<UserConfig> = {
  cliVersion: '',
  googleUsername: '',
  googleAccessToken: '',
  lastUpdatedAt: '',
};
